{
  "name": "@mr-burns/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bash ./scripts/ensure-electrobun-local-dep.sh && bunx electrobun dev",
    "build": "bash ./scripts/ensure-electrobun-local-dep.sh && bunx electrobun build --env=stable",
    "build:canary": "bash ./scripts/ensure-electrobun-local-dep.sh && bunx electrobun build --env=canary",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@mr-burns/shared": "workspace:*",
    "electrobun": "^1.15.1"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "~5.9.3"
  }
}
